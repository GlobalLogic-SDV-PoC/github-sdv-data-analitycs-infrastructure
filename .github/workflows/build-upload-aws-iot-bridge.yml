---
name: Build aws-iot-bridge and Upload to S3

on:
  pull_request:
    branches: [main]
    types: [closed]

permissions:
  id-token: write
  contents: read

jobs:
  build-and-upload:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository with submodules
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.GH_PAT }}

      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y build-essential libtool autoconf unzip wget \
            libssl-dev libssh-dev ninja-build pkg-config cmake

      - name: Build aws-iot-bridge
        run: |
          ./build.sh

      - name: Configure AWS credentials (OIDC → AssumeRole)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: arn:aws:iam::410849528398:role/GitHubActionsArtifactUploaderRole
          aws-region: us-east-2

      - name: Upload aws-iot-bridge to S3
        run: |
          if [ -f ./build/aws_iot_bridge_app ]; then
            aws s3 cp ./build/aws_iot_bridge_app \
              s3://nvidia-edge-to-cloud-bucket/build_artifacts/aws_iot_bridge_app \
              --region us-east-2
          else
            echo "aws-iot-bridge not found, skipping upload."
          fi